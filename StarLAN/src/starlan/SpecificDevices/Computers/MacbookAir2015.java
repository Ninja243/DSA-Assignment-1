/* This is an example of how we can simulate specific devices. The device being specified here is a Macbook Air (2015) laptop */
package starlan.SpecificDevices.Computers;

import starlan.GenericClasses.Device;
import starlan.GenericClasses.serverNode;
public class MacbookAir2015<AnyType> extends Device{
    
    // We assume that the laptop will send and recieve messages slightly slower
    // than a computer would be able to do, therefore a delay is added
    
     
}
